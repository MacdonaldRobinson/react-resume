{
  "compilerOptions": {
    "target": "esnext",                            // Use modern JavaScript features
    "module": "esnext",                            // Use dynamic import syntax for ES modules
    "moduleResolution": "node",                    // Resolve modules using Node's module resolution strategy
    "jsx": "react-jsx",                            // Enable JSX for React 17+ (automatic JSX transform)
    "noEmit": true,                                // Don't emit files, let Vite handle the build
    "esModuleInterop": true,                       // Enable default imports from CommonJS
    "skipLibCheck": true,                          // Skip library file type checking for faster build
    "strict": true,                                // Enable strict type-checking options for better safety
    "resolveJsonModule": true,                     // Allow importing JSON files as modules
    "forceConsistentCasingInFileNames": true       // Enforce consistent casing for file imports
  },
  "include": [
    "src/**/*"                                     // Include all source files under `src/`
  ],
  "exclude": [
    "node_modules"                                 // Exclude `node_modules` directory
  ]
}
